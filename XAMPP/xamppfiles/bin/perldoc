#!/Applications/XAMPP/xamppfiles/bin/perl

eval 'exec /Applications/XAMPP/xamppfiles/bin/perl  -S $0 ${1+"$@"}'
    if 0; # not running under some shell

require 5;
BEGIN { $^W = 1 if $ENV{'PERLDOCDEBUG'} }
use Pod::Perldoc;
exit( Pod::Perldoc->run() );

